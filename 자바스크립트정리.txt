클린코드
코드를 깔끔하게 쓰는법

패턴
https://www.patterns.dev/

플래시개발자가 원래 더 유명했다.

어도비 플래시는 html5가 나오고 망했다.
html은 자바스크립트로 해야한다.
ai도 자바스크립트로 돌아가고 결과물도 웹형태인 경우가 많다.

표준언어 이름을 제정을 ECMAScript 로 하게 되었다.
- **JavaScript** — 언어 및 그 구현
- **ECMAScript** — 언어 표준 및 버전
이름이 많다.

**ECMAScript**  사이트
https://tc39.es/ecma262/

옛날 기능을 유지하면서 새로운 버전을 제공
모던자바스크립트( ~ ES5(2009년 개발) 이후 부터를 지칭한다.)를 기준으로 학습을 한다.

자바스크립트는 웹, 내컴퓨터의 환경에서 살 수 있다.

자바스크립트는 vscode에서 수정할 수있고, 웹의 console에서도 수정할 수있다.


웹에서는 process 기능를 읽지 못하고 pc에서는 가능하다.
-웹과 서버는 서로 환경이 완전히 다르다 늘 생각하고 형성해야함.

자바스크립트
웹에서 쓸때는 = 브라우저의 정보
PC에서 쓸때는 = PC에 대한 정보

$ npm i -D nodemon
nodemon은 계속관찰되고 있어서 변화가 있으면 반영해준다.

$ npm i -D live-server

라이브서버를 여는 법이 다른 이유는?
npm은 패키지이고 


라이브서버는 앞으로 "npm run dev"로 실행, go live로 하면 이미지 경로에 대한 문제가 있다.

자바스크립트는 클라이언트환경, 서버환경에서 여는게 있다.
 "npm run dev"는 둘다 아니고 node_modules 디렉터리에서 불러온다

ESLint
lint:보풀

eslint가 옛날것이 좀더 더럽다. 회사가면 옛날것인 것이 많다.

shift + alt + A : 영역주석
ctrl + / : 한줄주석

------ 협업시 먼저 ESLint, prettier 설정은 고정해야함 --------

주석처리해서 eslint의 기능을 조절할 수있다.
- /* eslint no-unused-vars:1 */  : 전체적용
- // eslint-disable-next-line no-unused-vars : 오류인 부분의 커서를 올리고 빠른수정
- eslint.config.mjs 의 export default defineConfig 항목에  ** 꼭 최하단에 들어가야 한다
    rules:{
      'no-unused-vars':'warn'
    }
추가한다.
- ESLint 홈페이지의 Playground 으로 들어가서 세팅할 수 있다.


 npm i -D prettier 실행

.prettierrc.cjs 는 기준이 되는 파일이다.

singleQuote : 더블로 입력되지 않으므로 더블로 입력하려면 shift를 같이 눌러야 한다.


09/21

자바스크립트인포

서버 - 노드js

엔진은 v8, SpiderMonkey가 있다.

V8 – Chrome과 Opera에서 쓰입니다.
SpiderMonkey – Firefox에서 쓰입니다.

CORS(동일출처정책)을 많이 신경써야할 것

자바스크립트만의 강점
- HTML/CSS와 완전히 통합할 수 있음
- 간단한 일은 간단하게 처리할 수 있게 해줌
- 모든 주요 브라우저에서 지원하고, 기본 언어로 사용됨

사이트 콘솔에서 사용하는 것이 자바스크립트임.

명시적 vs 암시적
모두가 알아 들을 수있게
알아서 우리가 모르게 처리하는 기능

let message = 'hello'

let 선언
message = 'hello' 할당

코드에서는 = 할당연산자로 우변의 값을 좌변에 넣겠다는 의미이다.

함수형 프로그래밍 vs 객체형 프로그래밍 vs 함수 중심형 프로그래밍

함수형 프로그래밍 과 함수 중심형 프로그래밍은 다르다.

-변수명을 만들 땐 카멜 표기법(camelCase)이 흔히 사용,
-pascal형태는 사용하면 안된다. UserName 변수가 아닌 다른 의미로 받아들일 수 있기 때문
-프로그래밍을 할때 숫자가 앞에 오면 안된다.

보통 const로 개발을 하다가 필요할 때, let을 써야함

소문자로 설정, 대문자로 설정
-런타임 과정에서 계산되지만 최초 할당 이후 값이 변하지 않는 상수 : 소문자로 설정
- 코드가 실행되기 전에 이미 그 값을 알고 있는 상수 : 대문자로 설정


개발자들이 boolean 데이터가 존재한다고 가정하는 것 is

html5 버전에서 호환성쪽으로 발전되어온 것

010 이라고 입력하면 8진수로 인식한다.

Strict Mode vs Sloppy Mode
- 우리는 Sloppy Mode를 사용하고 있다.

 전역환경(객체, global object)
- 콘솔창, vscode 창을 전역환경이라 한다.
- 보통 전역환경은 window를 뜻한다.
- 전역에서 this를 찾는다면 

브라우저 환경에선 전역 객체를 window, Node.js 환경에선 global
- 윈도우 개발자는 보통 window
- 노드 개발자는 보통 global

선언은 전역에 속하도록 되어있다.

변수없이 선언 한것도 전역(window)에 귀속된다.

let이랑 const를 사용해라, var를 사용했을때, 변수없이 선언했을때 예외가 발생하니 반드시 사용해라

ex)
var a = 10;
delete window.a // false

b=10;
delete window.b // true

let c = 30; 윈도우 X / 변수 선언 & 할당

전역 객체를 사용하면 어디서나 사용 가능한 변수나 함수를 만들 수 있습니다. 

코딩은 늘 전역을 오염시키지 말자! - window에 직접입력하지 말고, const나 let을 

전역을 오염시키는건 대형 개체들이고, 우리는 그 작은 스코프 안에서 작업을 하는 것이다.

{} : 코드블록
스코프 어디까지 들여다 볼 수 있는가?

블록스코프 vs 함수스코프
- 함수안에 두면 꺼내쓸 수가 없다.

- var로 선언한 변수의 스코프는 함수 스코프이거나 전역 스코프입니다
- var는 함수스코프 안에 갇히게된다.
- let, const 는 블록스코프이다.

전역실행 컨텍스트
var 변수의 경우 : 어휘적환경 - 환경설정 - 오브젝트 환경(전역객체 : 윈도우) - 오브젝트 바인딩
let, const 변수 : 어휘적환경 - 환경설정 - 선언적 환경(let message;) - 아무런 일도 일어나지 않음
함수의 경우 : 어휘적환경 -환경 설정 - 오브젝트 환경 - 오브젝트 바인딩(window.)

var를 선언했을때는 호이스팅(코드의 위로 올려서 선언)되고 초기화되고, undefined 값이 메모리에 할당
let과 const는  호이스팅이 되고, 선언부 이전에는 사용할 수 없다. 

b라는 기호에 let으로 선언해줘도 TDZ(TemporalDeadZone) 이라고 부르고, 값이 없는 상태를 지칭함.

함수 실행 컨텍스트는 함수가 실행될때 만들어진다.

